export const add = (arg1, arg2) => {
  // assert true
  // assert arg1 > 5
  // some comment 1
  const foo = 1;
  // assert false
  // some comment 2
  // assert arg2
  const bar = 10;

  const result = arg1 + arg2;
  // assert result
  return result;
};
